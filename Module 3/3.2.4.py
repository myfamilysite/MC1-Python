# Define the string to iterate through
word = "Python"

# Iterate through each letter in the word
for letter in word:
    # If the letter is "o", do nothing and continue
    if letter == "o":
        pass
    else:
        # Print the letter if it is not "o"
        print(letter)

